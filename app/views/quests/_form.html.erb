<%= form_for(@quest) do |f| %>
  <% if @quest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quest.errors.count, "error") %> prohibited this quest from being saved:</h2>

      <ul>
      <% @quest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<tr>
	<td><strong><%= f.label :name %></strong><br />
    <%= f.text_field :name %></td>
	</tr>
	<tr>
    <td><strong><%= f.label :Catergory %></strong><br />
    <%= f.text_field :cat %></td>
	</tr>

<% end %>
